fun f (a int, b int, c int, d int, e int, g int, h int, i int) -> (int, int, int) {
    return (a + b, c + d, e + g + h + i);
}

fun main () -> int {
    let r = f(1, 2, 3, 4, 5, 6, 7, 8);
    return r.0 + r.1 + r.2;
}
