fun doubleAndCall(f (int, int) -> int, x int, y int) -> int {
    return f(x*2, y*2);
}

fun add(x int, y int) -> int {
    return x + y;
}

fun main () -> int {
    return doubleAndCall(add, 2, 3);
}