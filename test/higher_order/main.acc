fun add (x int, y int) -> int {
    return x + y;
}

fun adderFactory (x int) -> fun (int) -> int {
    return fun (y int) { return x + y; };
}

fun main () -> int {
    let inc = adderFactory(1);
    return inc(3);
}