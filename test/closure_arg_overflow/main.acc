fun main () -> int {
    let base = 1;
    let f = fun (a int, b int, c int, d int, e int, g int, h int, i int) -> int {
        return base + a + b + c + d + e + g + h + i;
    };
    return f(1, 2, 3, 4, 5, 6, 7, 8);
}
